<%# contentFor() is defined in express-ejs-layouts %>
<%- contentFor('body') %> 
<h3>This is manage-category page </h3>
<h4>All category: </h4>

<ul>
    <% rows.forEach(row => { %>
        <li><%= row.name %> </li>
    <% }); %>
</ul>

<%# relative path with this file in include() %>
<%- include('../some_widget'); %> 